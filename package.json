{
  "scripts": {
    "dev": "browser-sync start --server --files '**/*.html,**/*.css,**/*.js' --port 3000"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}
